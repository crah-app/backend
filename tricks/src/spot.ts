import { Word } from './word';

// The values represent the percentages
export enum Spot {
    Flat = 0.5,
    IntoBank = 0.3,
    DropIn = 0.2,
    Air = 0.0,
    Flyout = 0.0,
    OffLedge = 0.3
}

